var a:str = "Hello world"
cons b:i32 = 2 
var c:i32 = 0 

rep {
    c += 1
    println!("{}",a)
} until c >= b

var d = ["1", "2"]

for d as x {
    println!("{},{}","Loop time", x)
}

func e(x:str,x1:str) {
    println!("{},{}",x1, x)
}
//Un comentario x linea por ahora
//funcion de to_string() de rust, despues la saco
call e(&1.to_string(),&2.to_string())

func f(x1:i32,x:i32){
    ret x1 + x:i32
}

var g = f(2,4)

println!("{}",g)