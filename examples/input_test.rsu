package "\example packpages\input.rsup"
package "\example packpages\wait.rsup"
package "\example packpages\print.rsup"

//crea func main() primero para crear funciones globales, esto nos da para elegir entre "global var" y "var", lo mismo con las constantes, si estaran dentro o fuera de una funcion
//NO SE RECOMIENDA UTILIZAR GLOBAL VAR, SON INESTABLES
//LAS VARIABLES A PARTIR DE AHORA SE CREAN DENTRO DE LAS FUNCIONES

func create_tower() {
  var time: i64 = 2
  var test: i32 = 2147483647
  var times: u64 = 1
  print("Creating tower in 3..")
  wait(1000)
  print("Creating tower in 2..")
  wait(1000)
  print("Creating tower in 1..")
  wait(1000)
  print("Creating tower")
  wait(1000)
  rep {
    print(&time.to_string())
    wait(5*times)
    time += time
    times += 1
    //when it reaches the 64 bits limit it just stops
  }
}

func color_green() {
  var input = get_input()
}

func create_tower32() {
  var time: i32 = 2
  var times: u64 = 1
  print("Creating tower in 3..")
  wait(1000)
  print("Creating tower in 2..")
  wait(1000)
  print("Creating tower in 1..")
  wait(1000)
  print("Creating tower")
  wait(1000)
  rep {
    print(&time.to_string())
    wait(5*times)
    time += time
    times += 1
    //when it reaches the 32 bits limit it just stops
  }
}

func main() {
  var input = get_input()
  if input == "64tower" or input == "tower" {
      call create_tower()
    } else if input == "tower32" {
      call create_tower32()
    } else if input == "green" {
      call color_green()
    }
    print(&input)
  
}
